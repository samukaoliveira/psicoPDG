<%= form_with(model: inscricao) do |form| %>
  <% if inscricao.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inscricao.errors.count, "error") %> prohibited this inscricao from being saved:</h2>

      <ul>
        <% inscricao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :palestra_id, style: "display: block" %>
    <%= form.select :palestra_id, Palestra.all.map { | pl | [ pl.titulo, pl.id ] } %>
  </div>

  <div>
    <%= form.label :participante_id, style: "display: block" %>
    <%= form.select :participante_id, Participante.all.map { | pt | [ pt.nome, pt.id ] } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
